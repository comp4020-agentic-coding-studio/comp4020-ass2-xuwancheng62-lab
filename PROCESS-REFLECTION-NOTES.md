# Process and reflection working notes

记录日期：2026-09-12。项目阶段：前三周课程内容基本完成，正在按学生的学习顺序复查。

本文档保存项目过程中的设计决定、理由、证据入口与待验证问题，供项目完成后生成 process 和 reflection。它不是最终提交稿，也不代表已经取得学习成效。后续补充应区分已完成、已决定和待验证，不把计划写成成果，不虚构学生反馈或测试结果。

## 1. Week 1：控制生成变量，让练习聚焦导演判断

### 问题与设计决定

原先让学生独立生成 Take A 和 Take B，难以稳定保持人物、场景、构图、道具、灯光和时间等条件。两次生成的差异可能来自模型波动，学生很难判断 redirect 是否针对性地改善了表演。

Workshop 因此改为教师提供固定的 Take A、anchor image、base prompt 和生成设置。学生先诊断一个可观察的表演问题，再写自己的 Note B。工具可靠时用 remix/video-to-video 应用 redirect；否则从教师提供的 Take B 候选中选择最符合自己诊断的一版，并说明理由。伙伴只看视频，对变化及其解释作独立判断。

### 适合写入 process 的内容

- 记录从独立双次生成到教师固定素材的练习重设计。
- 说明固定哪些条件，以及学生仍需独立完成哪些判断。
- 保存素材 brief、诊断、Note B、候选片段及选择理由，展示设计如何落实。

### 适合讨论的 reflection

AI 的生成能力与课程的学习目标需要分别考虑。变量过多时，生成差异不能直接作为导演决定有效的证据。准备固定素材有助于把有限课堂时间用于诊断、redirect 和评价，但也增加教师准备成本。候选片段路径扩大了工具可用性，同时需要避免答案过于明显，或让选片完全代替实际执行 redirect。

### 当前状态与证据

Workshop 文字已重设计；截至本次记录，BIS-W01-WORKSHOP 仍为 NOT STARTED，等待重新生成和验收。页面已有可播放片段不等于整套教学素材已通过验收，不能写成课堂试验成功。

证据入口：

- `src/content/sessions/01-getting-started.md`
- `resources/week-01/workshop-brief.md`
- `PLAN.md` 的 Week 1 状态与修订记录
- `MANIFEST.md` 的 BIS-W01-WORKSHOP

### 项目完成前补充

记录最终素材是否保持必要条件、候选差异是否可诊断、练习能否在规定时间内完成，以及伙伴能否看出 redirect 的作用。没有实际试做证据时，保留为待验证问题。

## 2. Week 3：用模板解决新手不知道如何开始的问题

### 问题与设计决定

用户在阅读内容后反馈：“剧本到底怎么写，我看了以后还是没有头绪”，随后指出“有模版的话填写就容易得多”。这属于项目设计者按学生视角阅读时的反馈，不是外部学生测试。

此前材料较清楚地说明合格剧本与导演注释的标准，但对如何从空白开始写第一版支持不足。因此加入连续的写作过程：premise → decisive scene → scene change → five observable beats → screenplay form → director's note，并提供可下载的填写式 worksheet，让学生把自己的 Week 2 premise 转成场景。

Worksheet 是形成作品的支架，不作为单独评分的提交物；最终仍检查场景、有效的前置情境注释和伙伴对注释的理解。

### 适合写入 process 的内容

- 保存原先的阅读困惑，说明它如何触发修改。
- 展示从完成范例到逐步构建范例的教学变化。
- 保存 worksheet 及一次真实填写过程，标出仍会卡住的字段。

### 适合讨论的 reflection

明确评价标准与帮助新手生成作品是两项不同的教学任务。完成的范例可以展示终点，却不一定让学生知道第一步怎样走。模板把抽象判断拆成可填写的决定，但固定五个 beats 也可能被误解为所有场景都必须遵循的公式；后续应观察它是否帮助起步，以及何时需要放开支架。

### 当前状态与证据

写作过程和 worksheet 已加入。复查时发现 lecture/deck 要求课前填写，而 workshop 要求带空白版，说明新增支架还需要同步使用时机。本次对话已形成修正 prompt：课前下载阅读，课中填写；此记录不宣称该修正已实施。

证据入口：

- 本次对话中的用户阅读反馈
- `src/content/lectures/week-03.md`
- `src/content/sessions/03-screenplay-workshop.md`
- `src/decks/week-03.deck.mdx`
- `resources/week-03/scene-planning-worksheet.txt`
- `resources/week-03/materials.md`

### 项目完成前补充

亲自用一个自己的 premise 填写 worksheet，记录耗时、空白字段、需要额外解释的地方，以及能否据此独立写出第一版场景。将自我试做与真实学生反馈分别标注，不提前声称模板改善了学习效果。

## 3. AI 协作：限制一致性检查范围，保留人的范围决定权

### 问题与设计决定

项目规模增加后，重复启动全项目一致性检查及 subagent 会消耗大量时间，也可能重复发现已知问题。用户因此明确决定：全项目一致性检查启动前必须取得其同意；自动检查某一周时，只捎带前后一周的内容。

例如 Week 3 的局部一致性检查覆盖 Weeks 2–4。该限制同时适用于主 agent 与 subagent。范围外的问题先记录，扩大检查前再征得用户同意；复用已有发现，仅在修改可能使结论失效时重查。常规构建或类型检查应与全课程内容审查区分。

### 适合写入 process 的内容

- 记录用户怎样识别重复检查的成本，并明确检查边界。
- 保存检查请求、实际范围和已发现问题，减少重复劳动。
- 记录最终全项目审查是否由用户明确授权，以及它发现了哪些局部检查未覆盖的问题。

### 适合讨论的 reflection

更多自动检查并不必然带来更高的项目质量。检查深度、范围、重复成本和时机都需要管理。相邻周检查适合日常迭代，但无法保证发现相隔较远的依赖；全项目审查仍有价值，其启动时机由用户决定。节省下来的时间可以用于学生顺序试做和关键设计判断。

### 当前状态与证据

用户已在对话中批准此规则，Codex 已明确同意遵守。已提供供 CC 写入现有项目指令的 prompt；本次未验证所有 agent 指令是否已同步，不能把规则落地情况写成已验证成果。

证据入口：

- 本次对话中的检查范围决定与确认
- `CLAUDE.md` 的 Curriculum review 规则（最终写作时核实其最新版本）
- 后续实际 review 的范围与结果记录

### 项目完成前补充

如有记录，补充避免了哪些重复检查、局部检查遗漏了什么，以及最终完整检查的收益。没有计时数据时，不编造节省的分钟数或效率百分比。

## 4. 上下文管理：用每周短交接保留决定与工作状态

### 问题与设计决定

用户说明，CC 开始新一周内容时通常会清理上下文；涉及其他周的工作优先交给 Codex。这样可以让单周任务保持聚焦，但两个 agent 不会自动共享完整对话，新会话也可能遗失此前确认的要求。

Week 3 workshop 没有按照先前要求分成 Activity，是一次具体的要求遗漏。它促使用户提出：每周共同适用的要求应写入项目级 spec，不能只依赖聊天记忆。这里的 spec 指项目 authoring/workflow 规范，不是 session frontmatter 中描述学生完成标准的 `spec` 列表。

用户已同意采用每周短交接记录：在完成一周、转交任务或准备清理上下文时，更新该周的一份简短记录，供新 CC 会话和跨周接手的 Codex 共用。项目规范保存长期规则，handoff 保存本周状态和下一步；后者引用前者，避免多处复制规则后发生漂移。

### 交接记录应包含什么

- 当前状态：已完成、未完成、阻塞和下一步。
- 已确认决定：教学重点、用户批准的约束，以及有必要保留的否决理由；明确区分建议与已决定事项。
- 文件范围：本周主要文件、相关相邻周及原因、已知的其他 agent 正在修改的文件。
- 验证边界：检查了什么、结果如何、哪些后续修改尚未重查、已知问题。
- 新会话入口：必读文件与第一个具体任务，引用 `PLAN.md`、`MANIFEST.md` 等权威记录。

每周维护一份、控制在约一页内，更新当前状态而不是累积聊天流水账。新会话先读项目规范和相关 handoff，再核对当前文件；遇到记录过时或实质冲突时，应澄清而不是默默改写已确认决定。

### 适合写入 process 的内容

- 记录“每周清理 CC 上下文、跨周任务优先由 Codex 处理”的实际协作方式。
- 展示 Activity 要求遗漏如何促成项目规范与单周交接的分工。
- 保存一次清理前的 handoff，以及新会话如何据此恢复工作，说明文件记录怎样承接任务。

### 适合讨论的 reflection

清理上下文可以减少无关讨论，但也需要主动保存仍有效的决定。交接把任务连续性从对聊天记忆的依赖，转移到可检查、可更新的项目记录。其质量取决于信息是否精简、准确、及时更新；过长的记录会重新制造上下文负担，过时的记录则可能误导新会话。

项目规范与 handoff 解决不同问题：规范防止重复要求丢失，handoff 帮助接手当前工作。两者不能代替阅读当前文件，也不能保证两个 agent 不发生编辑冲突，因此仍需说明工作范围与已知并行任务。

### 当前状态与证据

用户已报告采用按周清理上下文和跨周分工，并已批准短交接流程。已提供给 CC 的实施 prompt，包含项目级规则、统一模板和 Week 3 handoff。本次仅补充 notes，没有验证模板、规范更新或实际 handoff 是否已落地，也没有新建这些文件；不能声称已证明交接节省时间或消除了遗漏。

证据入口：

- 本次对话中的上下文清理做法、handoff 确认与 Activity 遗漏反馈
- 本次对话中供 CC 实施每周交接与项目规范的 prompt
- 后续实际建立的项目规范、handoff 模板和每周记录（最终写作时核实文件路径与版本）

### 项目完成前补充

记录至少一次真实的跨会话接手：新会话是否能找到下一步、是否需要重复解释、是否发现过期状态、是否避免重做已有检查，以及长期规则是否仍被遵守。没有实际记录时，不把预期收益写成已验证成果。

## 5. 素材命名：学生看到的文字不能包含内部文件标识符

### 问题与设计决定

Week 6 lecture 中曾直接引用内部资产文件名，例如 `nadia-week1-reference-v1`、
`kitchen-week1-reference-v1`。这些名称来自 MANIFEST 资产追踪表，是给维护
课程内容的人看的编号，不是学生能理解的说明。用户通过截图指出学生不知道
这些代号指什么，并追问打算如何处理这些引用。

修正方式是把代号替换为面向学生的说明文字，并直接超链接到该说明所指的图
片本身（例如"Nadia 的外观参考"链接到对应 PNG），而不是解释代号的含义。
学生因此不需要知道内部命名规则，点开链接就能看到被引用的素材。

### 适合写入 process 的内容

- 记录截图反馈的具体内容和触发的修改。
- 保存修改前后的文本对比，说明代号如何被替换为说明性文字加超链接。
- 说明修改范围仅限于面向学生的文字，内部文件名本身、MANIFEST 记录未改动。

### 适合讨论的 reflection

内部追踪信息和学生可读说明是两套不同的语言，给内容维护者的编号体系不应
该原样出现在教学文字里，即使它对写作者而言是准确、可追溯的。这类问题不
容易通过自动检查发现，因为代号本身不是拼写或格式错误，只有从学生视角阅
读才会暴露。这提示以后新增素材引用时，应先问"学生看到这句话能不能行
动"，而不是只确认信息本身是否准确。

### 当前状态与证据

已修改 `src/content/lectures/week-06.md` 中相关段落，`pnpm build` 验证无
失效链接、无可访问性问题。改动记录在 `PLAN.md` 及 `handoffs/week-06.md`。

证据入口：

- 用户提供的截图反馈
- `src/content/lectures/week-06.md`（修改前后）
- `handoffs/week-06.md`

### 项目完成前补充

复查其余各周是否存在类似的内部代号直接出现在学生文字中的情况；目前未做
全课程范围检查，只在用户明确指出的地方修正。

## 6. 评估要求不能超过课程已经教过的标准

### 问题与设计决定

Assignment 1 的 `spec:` 条款曾要求"所有剧本需要的 bible 决定都已解决
（resolved）"，且明确写"没有任何决定仍处于 pending 状态"。但 Week 5 的
教学内容明确允许某个字段暂时标注为 pending 并说明还差什么决定，课程自己
的示范例子（Nadia 的声音设计）也一直保持 pending 状态，直到 Week 11 才
教声音表演相关技巧。也就是说，评估要求学生做到一件课程在 Assignment 1
截止时（第 6 周）还没教过的事。

用户指出后，修改方式是把 A1 的相关条款和说明文字改为"已解决，或明确标注
为 pending 并说明还缺什么决定"，与 Week 5 教的方式保持一致，而不是发明新
的、更严格的标准。

### 适合写入 process 的内容

- 记录用户反馈的原话，以及从 assignment-1.md 与 week-05.md 交叉核对得出
  的具体矛盾点（哪几条 spec、哪几句说明文字）。
- 保存修改前后的具体条款对比。
- 说明这是涉及评分标准（`spec:` 条款）的改动，先写入 PLAN.md 说明问题和
  方案，等待用户批准后才修改文件。

### 适合讨论的 reflection

评估标准应该回头对照课程实际教过什么，而不是独立设定一个"看起来更完整"
的目标。写评估条款时容易不自觉地写出比教学内容更高的要求，尤其是想强调
"作业要完整、可用"，反而忽略了这个"完整"是否真的在学生能力范围内。这类
问题需要显式地把评估条款和对应周的教学内容放在一起核对，单看评估文字本
身很难发现。

### 当前状态与证据

已修改 `src/content/assessments/assignment-1.md` 中 `spec:` 第 6、10 条
及正文相关段落，`pnpm typecheck`/`pnpm build` 均通过。改动记录在
`PLAN.md`（新增 "Assignment 1 — pending-decision alignment pass —
approved" 一节及修订记录）及 `handoffs/week-06.md`。

证据入口：

- 用户在对话中的反馈
- `src/content/assessments/assignment-1.md`（修改前后）
- `src/content/lectures/week-05.md` 中关于 pending 的教学段落
- `PLAN.md`、`handoffs/week-06.md`

### 项目完成前补充

检查其余各周的评估条款是否也存在类似"要求超过已教内容"的情况；本次修改
只处理了用户明确指出的 voice 相关条款，未做全课程范围审查。

## 7. 两类修改分开处理：措辞可以直接改，涉及评分标准的改动要先出方案等批准

### 问题与设计决定

项目的 `CLAUDE.md` 已经定了一条"trigger test"规则：凡是会改变某一周教/考
什么内容的改动（包括评分条款、学习目标），都必须先写入 PLAN.md 并取得用
户明确批准；而纯粹的措辞、链接、格式修改可以直接做。这次会话里实际按这
条规则走了两条不同的路径：修正 Week 6 里暴露的内部文件名，属于措辞问
题，直接修改；修正 Assignment 1 里对"声音设计"的强制要求，属于评分条款
改动，先在 PLAN.md 写清楚问题和修改方案，等用户确认后才动手改文件。

### 适合写入 process 的内容

- 并排记录这两次改动各自的处理过程，说明为什么一个直接改、一个先出方案。
- 保存 PLAN.md 中为 Assignment 1 改动写的方案文本，以及用户批准的时间点。
- 说明这条规则本身写在 `CLAUDE.md` 里，不是临时决定。

### 适合讨论的 reflection

同一个人负责判断"这算不算涉及评分标准"的改动，判断本身可能出错，尤其是
改动看起来很小（只改一两个词）但实际影响了评分条款的含义。这次的经验
是：遇到不确定的情况，按更谨慎的一边处理（先出方案），比自行断定"这只
是措辞"更安全。规则本身的价值也在于它把"要不要先问"这件事变成一个可检
查的标准（是否碰到 spec 条款、学习目标），而不是每次临时判断。

### 当前状态与证据

两次改动均已完成并通过 `pnpm typecheck`/`pnpm build`。Assignment 1 的方
案记录在 `PLAN.md` 的 "Assignment 1 — pending-decision alignment pass —
approved" 一节，Week 6 的措辞修改未单独立项，直接体现在改动后的文件与
`handoffs/week-06.md` 的记录里。

证据入口：

- `CLAUDE.md` 中的 "Planning and human–agent decision making" 一节
- `PLAN.md` 中本次两项改动的记录
- `handoffs/week-06.md`

### 项目完成前补充

如果后续再遇到难以判断"是否涉及评分标准"的边界情况，记录判断过程本身，
而不是只记录最终结论。

## 最终写作使用方式

Process 以“问题 → 决定 → 实施 → 验证”为主线，引用实际文件版本、片段和试做记录。Reflection 讨论这些决定背后的认识、取舍、局限，以及下一次会如何调整，不只复述修改清单。

生成最终稿前重新确认各项实施状态，补入项目完成后的证据，并保留尚未验证的限制。项目未完成前，本文档继续作为工作记录维护。
